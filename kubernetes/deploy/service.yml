apiVersion: v1
kind: Service
metadata:
  name: travel-expenses-tracker-backend
spec:
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: http
  selector:
    app: travel-expenses-tracker-backend
